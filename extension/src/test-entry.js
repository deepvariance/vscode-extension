// Bundled for tests so the assertions run against the same code VS Code loads.
export { toOpenAIMessages, toOpenAITools, DeepVarianceProvider } from './provider.js';
export { activate } from './extension.js';
